# MySQL connection string used by Prisma (the app and `npm run db:migrate`).
# Copy to .env for local development — .env is gitignored and never shipped.
DATABASE_URL="mysql://USER:PASSWORD@HOST:3306/DATABASE"
