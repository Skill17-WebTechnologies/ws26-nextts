# Production configuration, baked into the image and used when no DATABASE_URL is
# supplied by the environment. The entrypoint copies this file to .env on startup.
#
# The values below are placeholders — replace them with the real database.
DATABASE_URL="mysql://USER:PASSWORD@mysql.nstrim.app:3306/DATABASE"
